apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: shared-configs
  namespace: flux-system
spec:
  interval: 10m
  path: "./infrastructure/configs/shared"
  prune: true
  sourceRef:
    kind: GitRepository
    name: homelab
  targetNamespace: default
  dependsOn:
    - name: replicator
